<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">Sistema de Gerenciamento de Equipamentos</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Alternar navegação">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto d-flex flex-row">
                <li class="nav-item">
                    <a class="btn btn-primary" href="@Url.Action("GetAll", "Equipment")">GetAll</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-secondary" href="@Url.Action("GetById", "Equipment")">Buscar ID</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-success" href="@Url.Action("Adicionar", "Equipment")">Adicionar</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-warning" href="@Url.Action("Edit", "Equipment")">Editar</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-danger" href="@Url.Action("Delete", "Equipment")">Excluir</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-info" href="@Url.Action("ImportEquipments", "Equipment")">Importar CSV</a>
                </li>
            </ul>
        </div>
    </div>
</nav>